# Build the deck
genanki>=0.13
requests>=2.28
beautifulsoup4>=4.11

# Optional: the `--engine edge` fallback synthesiser in ltcard.py.
# The deck ships with LIEPA audio and does not need this.
# edge-tts>=6.1
